diff --git a/packages/cogtools-lib/src/index.test.ts b/packages/cogtools-lib/src/index.test.ts
index 8858872..2b0231c 100644
--- a/packages/cogtools-lib/src/index.test.ts
+++ b/packages/cogtools-lib/src/index.test.ts
@@ -1,7 +1,8 @@
 import { describe, it, expect } from 'vitest';
+import { getGreeting } from './index.js';
 
-describe('cogtools-lib', () => {
-  it('should pass a dummy test', () => {
-    expect(true).toBe(true);
+describe('getGreeting', () => {
+  it('should return the correct greeting', () => {
+    expect(getGreeting()).toBe('Hello from @cogtools/lib!');
   });
 });
diff --git a/packages/cogtools-lib/src/index.ts b/packages/cogtools-lib/src/index.ts
index 0d5ad04..51eefd0 100644
--- a/packages/cogtools-lib/src/index.ts
+++ b/packages/cogtools-lib/src/index.ts
@@ -1 +1,3 @@
-// Placeholder for cogtools-lib
+export function getGreeting(): string {
+  return 'Hello from @cogtools/lib!';
+}
diff --git a/packages/cogtools/src/index.test.ts b/packages/cogtools/src/index.test.ts
index aaf6c4c..ab72e0e 100644
--- a/packages/cogtools/src/index.test.ts
+++ b/packages/cogtools/src/index.test.ts
@@ -1,7 +1,22 @@
 import { describe, it, expect } from 'vitest';
+import { exec } from 'child_process';
+import { resolve } from 'path';
 
-describe('cogtools', () => {
-  it('should pass a dummy test', () => {
-    expect(true).toBe(true);
+const cliPath = resolve(__dirname, '../dist/index.js');
+
+describe('cogtools CLI', () => {
+  it('should output the greeting from lib', async () => {
+    const { stdout, stderr } = await new Promise<{ stdout: string; stderr: string }>((resolve, reject) => {
+      exec(`node ${cliPath}`, (error, stdout, stderr) => {
+        if (error) {
+          reject(error);
+        } else {
+          resolve({ stdout, stderr });
+        }
+      });
+    });
+
+    expect(stdout.trim()).toBe('Hello from @cogtools/lib!');
+    expect(stderr).toBe('');
   });
 });
diff --git a/packages/cogtools/src/index.ts b/packages/cogtools/src/index.ts
index d883150..6d21014 100644
--- a/packages/cogtools/src/index.ts
+++ b/packages/cogtools/src/index.ts
@@ -1 +1,10 @@
-// Placeholder for cogtools main CLI
+import { getGreeting } from '@cogtools/lib';
+
+export async function main() {
+  console.log(getGreeting());
+}
+
+// Standard pattern to make the module executable directly
+if (import.meta.url === `file://${process.argv[1]}`) {
+  main();
+}
